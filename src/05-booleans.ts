(() => {
  let isEnable = true
  // isEnable = 'Daniel'
  // isEnable = 123
  isEnable = false

  let isNew:boolean = true

  console.log(isNew);

  const random = Math.random();

  isNew = random >= 0.5 ? true : false

  console.log(isNew);


})()
